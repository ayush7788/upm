echo Uninstalling Universal Package Manager \"Wrapper\"
echo Run this script as root if not running as root
rm /usr/bin/upm
echo Removed upm from /usr/bin
rm -rf /usr/share/upm
echo Removed UPM config file
